# 📦 使用 GitHub Actions 自动编译

## 🎯 快速开始

### 1️⃣ 创建 GitHub 仓库

1. 访问 [GitHub](https://github.com/new)
2. 创建一个新仓库（可以是私有或公开）
3. **不要**勾选 "Initialize this repository with a README"

### 2️⃣ 推送代码到 GitHub

在你的项目目录中执行：

```bash
cd /mnt/c/Users/Administrator/Desktop/插件

# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: Hidden Album Toggle"

# 添加远程仓库（替换成你的仓库地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 3️⃣ 自动编译

推送后，GitHub Actions 会自动开始编译：

1. 访问你的仓库页面
2. 点击 **"Actions"** 标签
3. 查看编译进度

### 4️⃣ 下载编译好的 .deb 文件

编译完成后：

1. 在 Actions 页面找到最新的工作流运行
2. 点击进入详情页
3. 在 **"Artifacts"** 部分下载 `HiddenAlbumToggle-deb`
4. 解压得到 `.deb` 文件

---

## 🏷️ 创建发布版本（可选）

如果你想创建正式发布版本：

```bash
# 创建标签
git tag v1.0.0

# 推送标签
git push origin v1.0.0
```

GitHub Actions 会自动创建 Release，并附带 `.deb` 文件！

---

## 🔧 本地编译（需要 macOS 或配置好的 Linux）

如果你想在本地编译：

```bash
make clean
make package
```

编译好的文件在 `packages/` 目录下。

---

## 📱 安装到设备

1. 将 `.deb` 文件传输到你的 iOS 设备
2. 使用 Filza 或其他包管理器安装
3. 重启 SpringBoard：`killall -9 SpringBoard`

---

## ⚙️ 工作流说明

- **触发条件**：
  - 推送到 `main` 或 `master` 分支
  - 创建 Pull Request
  - 手动触发（在 Actions 页面点击 "Run workflow"）
  - 推送标签（会创建 Release）

- **编译环境**：Ubuntu + Theos + iOS SDK

- **产物**：`.deb` 安装包

---

## 🐛 故障排除

### 编译失败？

1. 检查 Actions 日志查看错误信息
2. 确保 `Makefile`、`control`、`Tweak.x` 等文件正确
3. 检查语法错误

### 无法推送到 GitHub？

```bash
# 检查远程仓库
git remote -v

# 如果需要重新设置
git remote set-url origin https://github.com/你的用户名/你的仓库名.git
```

---

## 📞 需要帮助？

如果遇到问题，请检查：
- GitHub Actions 日志
- Makefile 配置
- control 文件格式

